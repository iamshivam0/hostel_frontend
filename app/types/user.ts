export interface User {
  _id: string;
  firstName: string;
  lastName: string;
  email: string;
  role: string;
  roomNumber?: string;
  profileImage?: string;
  createdAt: string;
  updatedAt: string;
}
